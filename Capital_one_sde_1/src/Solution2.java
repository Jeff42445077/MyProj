import java.util.Collections;
import java.util.List;

public class Solution2 {
    public int time(List list, int zeroToOnes) {

        //if 1,0 && if zeroToOnes >0
        //list1=1111
        //list0 = 0000
        //1. if list0.size() >= zeroToOnes , list0.remove(list0.size()-zeroToOnes) and add 1 to list1
        //2. else if list1 != null && list0.size() < zeroToOnes, list1 -1, list0 + 0
        //3. else break
        //if 0,1 || if zeroToOnes <0, break
//        int i = 0;
//
//        while (list.size()!=1 && !list.get(0).equals(0) ) {
//            boolean x = list.contains(0) || list.contains(1);
//
////           if (zeroToOnes > 0 && x && (list.get(0) == 1
//            int n = Collections.frequency(list, 1);
//            List<Integer> list1 = Collections.nCopies(Collections.frequency(list, 1), 1);
//            List<Integer> list0 = Collections.nCopies(Collections.frequency(list, 0), 0);
//            if (list0.size() >= zeroToOnes) {
//                list0.remove(list0.size() - zeroToOnes);
//                list1.add(1);
//            } else if (list1 != null && list0.size() < zeroToOnes) {
//                list1.remove(list1.size() - 1);
//                list0.add(0);
//            }
//            i+=1;
//
//        }
//        return i;
//    }


        for (int i =0; i <)


}
